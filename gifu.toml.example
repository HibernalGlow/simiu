[output]
# gif | webp | apng | auto
format = "webp"
quality = 85

[naming]
# 默认前缀
prefix = "[#dyna]"
# 可用变量: {prefix} {stem} {archive} {parent}
template = "{prefix}{stem}"

[performance]
# 0 自动，建议根据 CPU 调整如 8 / 12
max_workers = 0
