[output]
# gif | webp | apng | auto
format = "webp"
quality = 85
# 每帧时长（毫秒）
duration_ms = 240
# 循环次数，0 为无限
loop = 0

[naming]
# 默认前缀
prefix = "[#dyna]"
# 可用变量: {prefix} {stem} {archive} {parent}
template = "{prefix}{stem}"
